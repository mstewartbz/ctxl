{
  "name": "@ctxl/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply ctxl-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply ctxl-db"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@ctxl/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
